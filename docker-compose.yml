services:
  navigatarr:
    build: .
    ports:
      - "7676:7676"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./data:/data
    environment:
      NAVIGATARR_HOST: homepage.joeysmith.com
      DB_PATH: /data/navigatarr.db
      NAVIGATARR_SHOW_SELF: "0"
